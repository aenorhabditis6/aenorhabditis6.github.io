import Observatory from "@/components/observatory/Observatory";

// The earlier take on the same material: one planet per project, read as a
// page rather than turned as a ring. Kept because the writing on each project
// lives here, and because it is the accessible route through the same work.
export default function Page() {
  return <Observatory />;
}
